function booWho(bool) {
    // if the type of value is boolean, return true
    if (typeof bool === "boolean") {
        return true
        // if not, return false
    } else {
        return false;
    }
}
console.log(booWho(false));
